## Goal

以 `https://courses.20060618.xyz/` 当前线上稳定页面为内容基线，只做增量上传本地已经跑好的新课程笔记，并同步把课程站改成白色模式。

## Current State

- 线上 `course-sitemap.xml` 当前包含 63 个课程详情页，已覆盖 `燃爆 / 互换 / 热传 / 法规 / 安化 / 工材 / 大物` 的旧课次。
- 线上详情页同时暴露 `index.md` 与部分 `clear.md`，内容可抓取，但它们是站点生成后的镜像 Markdown，不是原始 `_with_ima.md`。
- 本地课程源位于 `/Users/a123/Desktop/Obsidian-blog/03-领域/课程录音`，当前“有图可上站但线上还没有”的课次包括：
  - `互换/0507`
  - `互换/0514`
  - `大物/0506`
  - `大物/0513`
  - `工材/0519`
  - `热传/0423`
  - `热传/0507`
  - `热传/0512`
  - `热传/0521`
  - `安全学原理/0519`
- 当前生成器只认 `*_with_ima.md`，而本地新增页是 `*-note.illustrated.md`。
- 当前站点样式仍是深色主题。

## Gap

- 还没有一条可靠的“线上旧页回灌为 staging source”的链路。
- 还没有把本地新增完成页转换到旧生成器可读格式并与线上基线做差集合并。
- 还没有把白色模式实现到课程页与 Markdown 渲染公用样式。
- 还没有验证“旧页不变、新页出现”的本地构建结果。

## Success Standard

- 线上已有旧页内容保持不变，只新增本地已完成且线上缺失的课次。
- `互换/0514` 等新增页能在构建产物中出现，并保留插图。
- `安全学原理/0519` 能生成独立课程页。
- 全站切换为白色模式，并带轻量 grid / dot-grid 背景。
- 本地完成构建验证后，再增量发布到 `courses.20060618.xyz`。

## Constraints

- 禁止覆盖或弱化线上已经稳定落地的旧笔记内容。
- 不回退用户已有未提交改动。
- 不重新跑 OCR / MinerU；本次只处理站点源、构建与发布。
- 若某课次本地没有图，不作为“已跑好”上传对象。

## Objects / Entities

- 站点仓库：`/Users/a123/Desktop/Obsidian-blog/99-临时/courses_webui`
- 线上基线：`https://courses.20060618.xyz/`
- 线上 sitemap：`https://courses.20060618.xyz/course-sitemap.xml`
- 本地课程源：`/Users/a123/Desktop/Obsidian-blog/03-领域/课程录音`
- 构建产物：`dist-course-site/`

## Paths / Options

1. 直接用当前本地 Obsidian 课程源全量重建整站。
2. 抓取线上旧页作为稳定基线，再并入本地新增完成页后统一重建。
3. 手工逐页上传本地新增 Markdown 到线上。

当前选择第 2 条，因为只有它同时满足“旧页不动”和“新页增量上线”。

## Validation Method

- 用线上 sitemap 生成旧页键集合，与本地已完成页做差集，明确增量列表。
- 抽样核对构建后的旧页 HTML/Markdown 不出现重复标题或描述。
- 抽样核对新增页是否带图、`clear.md` 是否存在、首页课程计数是否增长。
- 发布后再抓线上页面做一次回归确认。
